# Benchmark results

Put local benchmark CSV files here when preparing a release or README update.

Suggested filename format:

```text
YYYY-MM-DD_machine_backend.csv
```

Do not commit private structured low-rank benchmark tables. Keep public results
focused on rank-one Cholesky update/downdate, direct `D @ v`, and `cholsolve`.
